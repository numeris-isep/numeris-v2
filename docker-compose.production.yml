version: "3.9"
services:

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    restart: unless-stopped
    ports:
      - 3000:80
    links:
      - mysql:db
    depends_on:
      - mysql

  mysql:
    image: mysql:5.7
    restart: unless-stopped
    env_file:
      - .env
    volumes:
    - mysql:/var/lib/mysql

  back:
    build:
      context: ./back
      dockerfile: Dockerfile
    env_file:
      - .env
    links:
    - mysql
    depends_on:
    - mysql

volumes:
  mysql:
